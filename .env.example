# Copy this file to .env and fill the values
NVIDIA_API_KEY=
# Optional: custom system prompt
SYSTEM_PROMPT=
# Optional: allow Werkzeug unsafe (for development only)
ALLOW_UNSAFE_WERKZEUG=0
# Port (optional)
PORT=5000
